# 🎭 Онлайн раздатчик карт для игры Мафия

Веб-приложение для автоматической раздачи ролей в игре Мафия с поддержкой онлайн комнат.

## Возможности

- ✅ Создание кастомных комнат с настройкой ролей
- ✅ Вход в комнату по ID или имени
- ✅ Автоматическая раздача ролей
- ✅ Просмотр игроков и их ролей для ведущего
- ✅ Отображение своей роли для игроков
- ✅ Работа в реальном времени через WebSocket

## Установка

### 1. Установите зависимости для сервера

```bash
npm install
```

### 2. Установите зависимости для клиента

```bash
cd client
npm install
cd ..
```

## Запуск

### Режим разработки (запускает сервер и клиент одновременно)

```bash
npm run dev
```

### Или запустите отдельно:

**Сервер:**
```bash
npm run server
```

**Клиент:**
```bash
npm run client
```

Сервер будет доступен на `http://localhost:5000`
Клиент будет доступен на `http://localhost:3000`

## Production

- **Сервер**: https://mafia-c6a2.onrender.com
- **Клиент**: https://hardcorechessorg.github.io/-/

Проект настроен для работы с этими адресами. См. `DEPLOYMENT.md` для инструкций по развертыванию.

### GitHub Pages авто-деплой

- `client/package.json` содержит `homepage`, указывающий на адрес GitHub Pages.
- Workflow `.github/workflows/pages.yml` при каждом пуше в `master`:
  - устанавливает зависимости в корне и в `client/`;
  - запускает `npm run build` внутри `client`;
  - публикует содержимое `client/build` в ветку `gh-pages`.
- В настройках GitHub Pages выберите ветку `gh-pages` и папку `/`.

## Использование

### Для ведущего:

1. На главной странице выберите "Я ведущий"
2. Настройте количество ролей:
   - Мафия
   - Дон Мафии
   - Комиссар
   - Доктор
   - Киллер
   - Мирный житель
3. Нажмите "Создать комнату"
4. Поделитесь ID комнаты с игроками
5. Дождитесь, пока все игроки присоединятся
6. Нажмите "Раздать роли" когда количество игроков совпадёт с количеством ролей
7. Просматривайте список игроков и их роли

### Для игрока:

1. На главной странице выберите "Я игрок"
2. Введите ID комнаты, который дал ведущий
3. Введите ваше имя
4. Нажмите "Войти в комнату"
5. Дождитесь раздачи ролей
6. Просмотрите вашу роль

## Технологии

- **Backend:** Node.js, Express, Socket.io
- **Frontend:** React
- **WebSocket:** Для работы в реальном времени

## Структура проекта

```
мафия/
├── server/
│   └── index.js          # Backend сервер с Socket.io
├── client/
│   ├── src/
│   │   ├── App.js        # Главный компонент
│   │   ├── components/
│   │   │   ├── HostPanel.js    # Панель ведущего
│   │   │   └── PlayerPanel.js  # Панель игрока
│   │   └── ...
│   └── ...
├── package.json
└── README.md
```

